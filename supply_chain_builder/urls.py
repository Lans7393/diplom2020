from django.urls import path

from . import views

app_name = 'main'
urlpatterns = [
    # path('', views.index, name='index'),
    # path('<int:company_id>/', views.detail, name='detail'),
    # path('<int:company_id>/add_activity/', views.add_activity, name='add_activity'),
]
